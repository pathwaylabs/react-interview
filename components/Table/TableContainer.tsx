import { styled } from '@mui/material';
import TableContainer from '@mui/material/TableContainer';

export const DefaultTableContainer = styled(TableContainer)`
  position: relative;
  max-height: 600px;
  width: 100%;
  overflow: auto;
  border-radius: 12px;
`;
